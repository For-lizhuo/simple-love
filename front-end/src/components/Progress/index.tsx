import * as React from "react";

import { CircularProgress } from "@mui/material";
import { Box } from "./style";
export const ProgressFC: React.FC = () => {
  return (
    <Box>
      <CircularProgress />
    </Box>
  );
};
